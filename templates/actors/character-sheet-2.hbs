{{!-- Components --}}
{{#*inline "pip"}}
    <button type="button" class="{{ classes }}" data-n="{{ n }}" data-tooltip="{{ tooltip }}" aria-label="{{ label }}"
            aria-pressed="{{ filled }}"></button>
{{/inline}}

{{#*inline "ability-score"}}
    <div class="ability-score {{#if flipped}}flipped{{/if}}" data-ability="{{ key }}">
        <a class="label {{ @root.rollableClass }} ability-check">{{ abbr }}</a>
        <div class="mod">
            {{#if @root.editable}}
            <a class="config-button" data-action="ability"
               data-tooltip="{{ localize "N5EB.AbilityConfigure" ability=label }}"
               aria-label="{{ localize "N5EB.AbilityConfigure" ability=label }}">
                <i class="fas fa-cog"></i>
            </a>
            {{else}}
            {{ n5eb-formatModifier mod }}
            {{/if}}
        </div>
        <div class="score">
            {{#if @root.editable}}
            <input type="text" name="system.abilities.{{ key }}.value" value="{{ baseValue }}" placeholder="10"
                   data-dtype="Number" class="uninput">
            {{else}}
            {{ value }}
            {{/if}}
        </div>
    </div>
{{/inline}}

<form class="{{ cssClass }} flexcol" autocomplete="off">

    {{!-- Header --}}
    <header class="sheet-header">

        {{!-- Name & Class --}}
        <div class="left optional-ability-{{ abilityRows.optional }}">

            {{!-- Name --}}
            {{#if editable}}
            <input type="text" name="name" class="document-name uninput" value="{{ actor.name }}">
            {{else}}
            <div class="document-name">{{ actor.name }}</div>
            {{/if}}

            {{!-- Class --}}
            <div class="class roboto-upper">{{ labels.class }}</div>
        </div>

        {{!-- Level, Inspiration, XP, & Buttons --}}
        <div class="right">

            <div class="willfire-level">
                <div class="level-badge badge" aria-label="{{ localize "N5EB.LevelNumber" level=system.details.level }}">
                    {{ system.details.level }}
                </div>

                <div class="pips" data-prop="system.attributes.inspiration">
                    {{#each willOfFire}}
                        {{> pip }}
                    {{/each}}
                </div>
            </div>
            {{!-- Level --}}


            {{!-- Inspiration --}}
            {{!-- <button type="button" class="inspiration unbutton" data-action="toggleInspiration"
                    aria-label="{{ localize "N5EB.Inspiration" }}" data-tooltip="N5EB.Inspiration"
                    aria-pressed="{{ system.attributes.inspiration }}">
            </button> --}}

     

            <div>
                {{!-- Resting & Special Traits --}}
                {{#if actor.isOwner}}
                <div class="sheet-header-buttons">
                    <button type="button" class="short-rest gold-button" data-tooltip="N5EB.ShortRest"
                            aria-label="{{ localize "N5EB.ShortRest" }}">
                        <i class="fas fa-utensils"></i>
                    </button>
                    <button type="button" class="long-rest gold-button" data-tooltip="N5EB.LongRest"
                            aria-label="{{ localize "N5EB.LongRest" }}">
                        <i class="fas fa-campground"></i>
                    </button>
                    <button type="button" class="config-button gold-button" data-action="flags"
                            data-tooltip="N5EB.SpecialTraits" aria-label="{{ localize "N5EB.SpecialTraits" }}">
                        <i class="fas fa-star"></i>
                    </button>
                </div>
                {{/if}}

                {{!-- XP --}}
                {{#unless disableExperience}}
                {{#with system.details.xp}}
                <div class="xp-label roboto-upper">
                    {{#if @root.editable}}
                    <input type="text" name="system.details.xp.value" class="value uninput" data-dtype="Number"
                           value="{{ value }}">
                    {{else}}
                    <span class="value">{{ n5eb-numberFormat value }}</span>
                    {{/if}}
                    <span class="separator">&sol;</span>
                    <span class="max">{{ n5eb-numberFormat max }}</span>
                </div>
                <div class="xp-bar" role="meter" aria-label="{{ localize "N5EB.ExperiencePointsLabel" }}"
                     aria-valuemin="{{ min }}" aria-valuenow="{{ value }}" aria-valuemax="{{ max }}"
                     aria-valuetext="{{ pct }}%" style="--bar-percentage: {{ pct }}%;"></div>
                {{/with}}
                {{/unless}}
            </div>
        </div>
    </header>

    {{!-- Body --}}
    <section class="sheet-body">

        {{!-- Main Content --}}
        <div class="main-content">

            {{!-- Sidebar --}}
            <div class="sidebar">

                {{!-- Character Card --}}
                <div class="card">

                    {{!-- Collapser --}}
                    <button type="button" class="sidebar-collapser collapser card-tab vertical unbutton interface-only"
                        {{#if sidebarCollapsed}}
                        data-tooltip="JOURNAL.ViewExpand" aria-label="{{ localize "JOURNAL.ViewExpand" }}"
                        {{else}}
                        data-tooltip="JOURNAL.ViewCollapse" aria-label="{{ localize "JOURNAL.ViewCollapse" }}"
                        {{/if}}>
                        <i class="fas fa-caret-{{#if sidebarCollapsed}}right{{else}}left{{/if}}"></i>
                    </button>

                    {{!-- Portrait --}}
                    <div class="portrait {{#if portrait.token}}token{{/if}}">
                        {{#if editable}}
                        <label class="slide-toggle roboto-upper">
                            <input type="checkbox" name="flags.n5eb.showTokenPortrait" {{ checked portrait.token }}>
                            {{#if portrait.token}}
                            <i class="fas fa-toggle-on"></i>
                            {{ localize "DOCUMENT.Token" }}
                            {{else}}
                            <i class="fas fa-toggle-off"></i>
                            {{ localize "N5EB.Portrait" }}
                            {{/if}}
                        </label>
                        {{/if}}

                        <img src="{{ portrait.src }}" alt="{{ actor.name }}"
                             {{#if (and editable portrait.path)}}data-edit="{{ portrait.path }}"{{/if}}>
                    </div>

                    {{!-- Stats --}}
                    <div class="stats">

                        {{!-- AC & Exhaustion --}}
                        <div class="top">

                            <div class="pips" data-prop="system.attributes.exhaustion">
                                {{!-- {{#each exhaustion.left}}
                                    {{> pip }}
                                {{/each}} --}}
                            </div>

                            {{!-- AC --}}
                            <div class="ac-badge badge" aria-label="{{ localize "N5EB.ArmorClass" }}">
                                {{#if editable}}
                                <a class="config-button" data-action="armor" data-tooltip="N5EB.ArmorConfig"
                                   aria-label="{{ localize "N5EB.ArmorConfig" }}">
                                    <i class="fas fa-cog"></i>
                                </a>
                                {{else}}
                                <div data-attribution="attributes.ac" data-attribution-caption="N5EB.ArmorClass"
                                     data-tooltip-direction="DOWN">
                                    {{ system.attributes.ac.value }}
                                </div>
                                {{/if}}
                            </div>

                            <div class="pips" data-prop="system.attributes.exhaustion">
                                {{!-- {{#each exhaustion.right}}
                                    {{> pip }}
                                {{/each}} --}}
                            </div>
                        </div>

                        {{!-- Initiative, Speed, & Proficiency --}}
                        <div class="lozenges">

                            {{!-- Initiative --}}
                            <div class="lozenge {{#unless editable}}{{ rollableClass }}{{/unless}}"
                                 {{#unless editable}}data-action="rollInitiative"{{/unless}}>
                                <div class="value">
                                    {{#if editable}}
                                    <a class="config-button" data-action="initiative"
                                       data-tooltip="N5EB.InitiativeConfig"
                                       aria-label="{{ localize "N5EB.InitiativeConfig" }}">
                                        <i class="fas fa-cog"></i>
                                    </a>
                                    {{else}}
                                    <span>
                                        {{ n5eb-numberFormat system.attributes.init.total signDisplay="always" }}
                                    </span>
                                    {{/if}}
                                </div>
                                <div class="label roboto-condensed-upper">{{ localize "N5EB.Initiative" }}</div>
                            </div>

                            {{!-- Speed --}}
                            <div class="lozenge" data-attribution="attributes.movement">
                                <div class="value">
                                    {{#if editable}}
                                    <a class="config-button" data-action="movement" data-tooltip="N5EB.MovementConfig">
                                        <i class="fas fa-cog"></i>
                                    </a>
                                    {{else}}
                                    {{ speed.value }}
                                    {{/if}}
                                </div>
                                <div class="label roboto-condensed-upper">{{ speed.label }}</div>
                            </div>

                            {{!-- Proficiency --}}
                            <div class="lozenge">
                                <div class="value">
                                    {{ n5eb-numberFormat system.attributes.prof signDisplay="always" }}
                                </div>
                                <div class="label roboto-condensed-upper">{{ localize "N5EB.Proficiency" }}</div>
                            </div>
                        </div>

                        {{!-- Hit Points --}}
                        <div class="meter-group">
                            <div class="label roboto-condensed-upper">
                                <span>{{ localize "N5EB.HitPoints" }}</span>
                                {{#if editable}}
                                <a class="config-button" data-action="hitPoints" data-tooltip="N5EB.HitPointsConfig"
                                   aria-label="{{ localize "N5EB.HitPointsConfig" }}">
                                    <i class="fas fa-cog"></i>
                                </a>
                                {{/if}}
                            </div>
                            {{#with system.attributes.hp}}
                            <div class="meter sectioned hit-points">
                                <div class="progress hit-points
                                     {{~#if (gt tempmax 0)}} temp-positive
                                     {{~else if (lt tempmax 0)}} temp-negative{{/if}}"
                                     role="meter" aria-valuemin="0" aria-valuenow="{{ value }}"
                                     aria-valuemax="{{ max }}" style="--bar-percentage: {{ pct }}%">
                                    <div class="label">
                                        <span class="value">{{ value }}</span>
                                        <span class="separator">&sol;</span>
                                        <span class="max">{{ effectiveMax }}</span>
                                        {{#if tempmax}}
                                        <span class="bonus">{{n5eb-numberFormat tempmax signDisplay="always"}}</span>
                                        {{/if}}
                                    </div>
                                    <input type="text" name="system.attributes.hp.value" data-dtype="Number"
                                           placeholder="0" value="{{ value }}" hidden>
                                </div>
                                <div class="tmp">
                                    <input type="text" name="system.attributes.hp.temp" data-dtype="Number"
                                           placeholder="{{ localize "N5EB.TMP" }}" value="{{ temp }}">
                                </div>
                            </div>
                            {{/with}}
                        </div>

                        {{!-- Chakra Points --}}
                        <div class="meter-group">
                            <div class="label roboto-condensed-upper">
                                <span>{{ localize "N5EB.ChakraPoints" }}</span>
                                {{#if editable}}
                                <a class="config-button" data-action="chakraPoints" data-tooltip="N5EB.ChakraPointsConfig"
                                   aria-label="{{ localize "N5EB.ChakraPointsConfig" }}">
                                    <i class="fas fa-cog"></i>
                                </a>
                                {{/if}}
                            </div>
                            {{#with system.attributes.cp}}
                            <div class="meter sectioned chakra-points">
                                <div class="progress chakra-points
                                     {{~#if (gt tempmax 0)}} temp-positive
                                     {{~else if (lt tempmax 0)}} temp-negative{{/if}}"
                                     role="meter" aria-valuemin="0" aria-valuenow="{{ value }}"
                                     aria-valuemax="{{ max }}" style="--bar-percentage: {{ pct }}%">
                                    <div class="label">
                                        <span class="value">{{ value }}</span>
                                        <span class="separator">&sol;</span>
                                        <span class="max">{{ effectiveMax }}</span>
                                        {{#if tempmax}}
                                        <span class="bonus">{{n5eb-numberFormat tempmax signDisplay="always"}}</span>
                                        {{/if}}
                                    </div>
                                    <input type="text" name="system.attributes.cp.value" data-dtype="Number"
                                           placeholder="0" value="{{ value }}" hidden>
                                </div>
                                <div class="tmp">
                                    <input type="text" name="system.attributes.cp.temp" data-dtype="Number"
                                           placeholder="{{ localize "N5EB.TMP" }}" value="{{ temp }}">
                                </div>
                            </div>
                            {{/with}}
                        </div>

                        {{!-- Hit Dice --}}
                        {{#with system.attributes.hd}}
                        <div class="meter-group">
                            <div class="label roboto-condensed-upper">
                                <span>{{ localize "N5EB.HitDice" }}</span>
                                {{#if @root.editable}}
                                <a class="config-button" data-action="hitDice" data-tooltip="N5EB.HitDiceConfig"
                                   aria-label="{{ localize "N5EB.HitDiceConfig" }}">
                                    <i class="fas fa-cog"></i>
                                </a>
                                {{/if}}
                            </div>
                            <div class="meter hit-dice progress" role="meter" aria-valuemin="0"
                                 aria-valuenow="{{ value }}" aria-valuemax="{{ max }}"
                                 style="--bar-percentage: {{ pct }}%">
                                <div class="label">
                                    <span class="value">{{ value }}</span>
                                    <span class="separator">&sol;</span>
                                    <span class="max">{{ max }}</span>
                                </div>
                            </div>
                        </div>
                        {{/with}}

                        {{!-- Chakra Dice --}}
                        {{#with system.attributes.cd}}
                        <div class="meter-group">
                            <div class="label roboto-condensed-upper">
                                <span>{{ localize "N5EB.ChakraDice" }}</span>
                                {{#if @root.editable}}
                                <a class="config-button" data-action="chakraDice" data-tooltip="N5EB.ChakraDiceConfig"
                                   aria-label="{{ localize "N5EB.ChakraDiceConfig" }}">
                                    <i class="fas fa-cog"></i>
                                </a>
                                {{/if}}
                            </div>
                            <div class="meter chakra-dice progress" role="meter" aria-valuemin="0"
                                 aria-valuenow="{{ value }}" aria-valuemax="{{ max }}"
                                 style="--bar-percentage: {{ pct }}%">
                                <div class="label">
                                    <span class="value">{{ value }}</span>
                                    <span class="separator">&sol;</span>
                                    <span class="max">{{ max }}</span>
                                </div>
                            </div>
                        </div>
                        {{/with}}

                    </div>

                    {{!-- Death Saves --}}
                    <div class="death-tray {{#if death.open}}open{{/if}}">
                        <div class="death-saves">

                            {{!-- Successes --}}
                            <div class="pips" data-prop="system.attributes.death.success">
                                {{#each death.success}}
                                    {{> pip }}
                                {{/each}}
                            </div>

                            {{!-- Roll Death Save --}}
                            <button type="button" data-action="rollDeathSave" data-tooltip="N5EB.DeathSaveRoll"
                                    aria-label="{{ localize "N5EB.DeathSaveRoll" }}"
                                    class="{{ rollableClass }} death-save unbutton">
                                <n5eb-icon src="systems/n5eb/icons/svg/statuses/dead.svg"></n5eb-icon>
                            </button>

                            {{!-- Failures --}}
                            <div class="pips" data-prop="system.attributes.death.failure">
                                {{#each death.failure}}
                                    {{> pip }}
                                {{/each}}
                            </div>

                        </div>

                        <button type="button" class="death-tab card-tab horizontal unbutton interface-only"
                                {{#if death.open}}
                                    data-tooltip="N5EB.DeathSaveShow" aria-label="{{ localize "N5EB.DeathSaveShow" }}"
                                {{else}}
                                    data-tooltip="N5EB.DeathSaveHide" aria-label="{{ localize "N5EB.DeathSaveHide" }}"
                                {{/if}}>
                            <i class="fas fa-skull"></i>
                        </button>
                    </div>

                </div>

                {{!-- Favorites --}}
                <div class="favorites">
                    <h3 class="icon">
                        <i class="fas fa-star"></i>
                        <span class="roboto-upper">{{ localize "N5EB.Favorites" }}</span>
                    </h3>
                    <ul class="unlist">
                        {{#each favorites}}

                        {{!-- Favorite --}}
                        <li class="{{#if itemId}}item-tooltip{{/if}} {{ type }} {{#if suppressed}}suppressed{{/if}}"
                            data-favorite-id="{{ id }}" {{#if key}}data-key="{{ key }}"{{/if}}
                            {{#if itemId}}data-item-id="{{ itemId }}"{{/if}}
                            {{#if effectId}}data-effect-id="{{ effectId }}"{{/if}}
                            {{#if parentId}}data-parent-id="{{ parentId }}"{{/if}}
                            {{#if reference}}data-reference-tooltip="{{ reference }}"{{/if}}
                            {{#if preparationMode}}data-preparation-mode="{{ preparationMode }}"{{/if}}
                            {{#if level}}data-level="{{ level }}"{{/if}}
                            {{#if toggle.applicable}}data-action="useFavorite"{{/if}}>

                            {{!-- Icon --}}
                            <figure>
                                <img class="gold-icon" alt="{{ title }}" src="{{ img }}">

                                {{!-- Favorite Deletion --}}
                                {{#if @root.editable}}
                                <a class="deletion-control" data-action="removeFavorite" data-tooltip-class
                                   data-tooltip-direction data-tooltip="N5EB.FavoriteRemove"
                                   aria-label="{{ localize "N5EB.FavoriteRemove" }}">
                                    <i class="fas fa-circle-xmark"></i>
                                </a>
                                {{/if}}
                            </figure>

                            {{!-- Name --}}
                            <div class="name-stacked {{ rollableClass }}"
                                 role="button" data-action="useFavorite">
                                <span class="title">{{ title }}</span>
                                {{#if subtitle}}
                                <span class="subtitle">{{{ subtitle }}}</span>
                                {{/if}}
                            </div>

                            {{!-- Info --}}
                            <div class="info">
                                <div class="primary {{ css }}">
                                    {{!-- Item Uses & Capacity --}}
                                    {{#if uses}}
                                    {{#with uses}}
                                    {{#if (and name @root.actor.isOwner)}}
                                    <input type="text" class="uninput value" value="{{ value }}"
                                           {{#unless ../bareName}}data-{{/unless}}name="{{ name }}"
                                           data-dtype="Number" inputmode="numeric" pattern="[0-9+=\-]*">
                                    {{else}}
                                    <span class="value">{{ value }}</span>
                                    {{/if}}
                                    <span class="separator">&sol;</span>
                                    <span class="max">{{ max }}</span>
                                    {{/with}}

                                    {{!-- Modifiers --}}
                                    {{else if modifier includeZero=true}}
                                    {{ n5eb-formatModifier modifier }}

                                    {{!-- Passive Score --}}
                                    {{#if passive}}
                                    <span class="passive">({{ passive }})</span>
                                    {{/if}}

                                    {{!-- Saves --}}
                                    {{else if save.dc}}
                                    {{#with save}}
                                    <span class="ability">
                                        {{ lookup (lookup @root.config.abilities ability) "abbreviation" }}
                                    </span>
                                    <span class="value">{{ dc }}</span>
                                    {{/with}}

                                    {{!-- Other Value --}}
                                    {{else if value includeZero=true}}
                                    <span class="value">{{ value }}</span>

                                    {{!-- Quantity --}}
                                    {{else if quantity}}
                                    <span class="sign">&times;</span>
                                    <span class="value">{{ quantity }}</span>

                                    {{!-- Toggleable --}}
                                    {{else if toggle.applicable}}
                                    <i class="fas fa-toggle-{{#if toggle.value}}on{{else}}off{{/if}}"></i>

                                    {{!-- Legacy Resources --}}
                                    {{else if resource}}
                                    {{#if @root.actor.isOwner}}
                                    <input type="text" class="uninput value" value="{{ resource.value }}"
                                           data-dtype="Number" name="system.{{ id }}.value" inputmode="numeric"
                                           pattern="[0-9+=\-]*">
                                    {{else}}
                                    <span class="value">{{ resource.value }}</span>
                                    {{/if}}
                                    <span class="separator">&sol;</span>
                                    {{#if @root.editable}}
                                    <input type="text" class="uninput max" value="{{ resource.source.max }}"
                                           data-dtype="Number" name="system.{{ id }}.max" inputmode="numeric"
                                           pattern="[0-9+=\-]*">
                                    {{else}}
                                    <span class="max">{{ resource.max }}</span>
                                    {{/if}}
                                    {{/if}}
                                </div>
                                <div class="secondary">
                                    {{!-- Quantity --}}
                                    {{#if (and uses quantity)}}
                                    <span class="quantity">&times; {{ quantity }}</span>

                                    {{!-- Range --}}
                                    {{else if range.value}}
                                    {{#with range}}
                                    <span class="range">
                                        {{ value }}
                                        {{#if long}}&sol; {{ long }}{{/if}}
                                        {{ units }}
                                    </span>
                                    {{/with}}
                                    {{/if}}
                                </div>
                            </div>

                        </li>

                        {{/each}}

                        {{!-- Drop Indicator --}}
                        <li class="drop roboto-upper">{{ localize "N5EB.FavoriteDrop" }}</li>
                    </ul>
                </div>
            </div>

            {{!-- Tabbed Content --}}
            <section class="tab-body">

                {{!-- Character Details --}}
                {{> "n5eb.character-details" }}

                {{!-- Inventory --}}
                <div class="tab inventory" data-group="primary" data-tab="inventory">
                    {{> "n5eb.inventory2" sections=inventory }}
                </div>

                {{!-- Features --}}
                <div class="tab features" data-group="primary" data-tab="features">
                    {{> "n5eb.creature-features" sections=features.sections filters=features.filters
                        showClassDrop=true }}
                </div>

                {{!-- Spells --}}
                <div class="tab spells" data-group="primary" data-tab="spells">
                    {{> "n5eb.creature-spells" }}
                </div>

                {{!-- Effects --}}
                <div class="tab effects" data-group="primary" data-tab="effects">
                    {{> "n5eb.active-effects2" }}
                </div>

                {{!-- Biography --}}
                {{~> "n5eb.character-biography" ~}}

            </section>

        </div>

    </section>

    {{!-- Ability Scores --}}
    <section class="ability-scores optional-ability-{{ abilityRows.optional }}">
        <div class="rows">
            <div class="top">
                {{#each abilityRows.top}}
                    {{> ability-score flipped=true }}
                {{/each}}
            </div>
            <div class="bottom">
                {{#each abilityRows.bottom}}
                    {{> ability-score }}
                {{/each}}
            </div>
        </div>
    </section>

    {{!-- Child Creation --}}
    <button type="button" class="create-child gold-button"
            aria-label="{{ "SIDEBAR.Create" type=(localize "DOCUMENT.Item") }}">
        <i class="fas fa-plus"></i>
    </button>

    {{!-- Warnings --}}
    {{> "n5eb.actor-warnings-dialog" }}

</form>
