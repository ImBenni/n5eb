<form autocomplete="off" data-id="{{_id}}" data-type="{{type}}">
    {{> "n5eb.advancement-controls"}}
    <div class="form-group">
        <label>{{localize "N5EB.AdvancementAbilityScoreImprovementCap"}}</label>
        <div class="form-fields">
            <input type="number" name="configuration.cap" value="{{configuration.cap}}" min="1" step="1"
                         placeholder="{{localize 'None'}}">
        </div>
        <p class="hint">{{localize "N5EB.AdvancementAbilityScoreImprovementCapHint"}}</p>
    </div>

    <ul class="ability-scores">
        {{> "n5eb.advancement-ability-score-control" points canAdjust=true}}
        <li class="hint">
            {{localize "N5EB.AdvancementAbilityScoreImprovementPointsHint"}}
        </li>
        {{#each abilities}}
        {{> "n5eb.advancement-ability-score-control" this canAdjust=true}}
        {{/each}}
    </ul>
</form>
