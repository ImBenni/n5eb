<form autocomplete="off">
    <h3>{{ localize "N5EB.DamageTypes" }}</h3>
    <p class="note">{{ localize "N5EB.DamageModification.Hint" }}</p>
    <ol class="trait-list modifications">
        {{#each modifications}}
        <li>
            <label>
                {{ label }}
                <input type="text" name="system.traits.dm.amount.{{ @key }}" value="{{ value }}"
            </label>
        </li>
        {{/each}}
    </ol>

    <h3>{{ localize "N5EB.DamagePhysicalBypass" }}</h3>
    <p class="note">{{ localize "N5EB.DamageModification.BypassHint" }}</p>
    {{> "n5eb.trait-list" choices=bypasses prefix="system.traits.dm.bypasses" }}

    <button type="submit" name="submit" value="1">
        <i class="fa-regular fa-save"></i> {{ localize "N5EB.TraitSave" }}
    </button>
</form>
