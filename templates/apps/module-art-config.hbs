<form>
    <ol>
        {{#each config}}
        <li data-id="{{id}}">
            <input type="hidden" name="{{ id }}.priority" value="{{ priority }}" data-dtype="Number">
            <label>{{label}}</label>
            <div class="form-fields">
                <label>
                    {{localize "N5EB.ModuleArtConfigPortraits"}}
                    <input type="checkbox" name="{{id}}.portraits" {{checked portraits}}>
                </label>
                <label>
                    {{localize "N5EB.ModuleArtConfigTokens"}}
                    <input type="checkbox" name="{{id}}.tokens" {{checked tokens}}>
                </label>
            </div>
            <div class="priority-adjust">
                <button type="button" data-action="increase"
                        aria-label="{{ localize "N5EB.ModuleArtPriorityIncrease" }}">
                    <i class="fas fa-angle-up"></i>
                </button>
                <button type="button" data-action="decrease"
                        aria-label="{{ localize "N5EB.ModuleArtPriorityDecrease" }}">
                    <i class="fas fa-angle-down"></i>
                </button>
            </div>
        </li>
        {{/each}}
    </ol>
    <p class="hint">{{ localize "N5EB.ModuleArtPriorityHint" }}</p>
    <footer class="sheet-footer flexrow">
        <button type="submit">
            <i class="far fa-save"></i> {{localize "Save Changes"}}
        </button>
    </footer>
</form>
