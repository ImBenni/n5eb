<div class="tab advancement" data-group="primary" data-tab="advancement">
    <section class="items-list">
        {{#each advancement}}

        <div class="items-section card" data-level="{{ @key }}">

            <div class="items-header header">

                <h3 class="item-name">
                    {{#if (eq @key "0")}}
                    {{ localize "N5EB.AdvancementLevelAnyHeader" }}
                    {{else if (eq @key "unconfigured")}}
                    {{ localize "N5EB.AdvancementLevelNoneHeader" }}
                    {{else}}
                    {{ localize "N5EB.AdvancementLevelHeader" level=@key }}
                    {{/if}}
                </h3>

                <div class="item-header advancement-value">{{ localize "N5EB.Value" }}</div>

                <div class="item-header item-controls">
                    {{#if (and @root.editable configured (ne @key "unconfigured"))}}
                    <a class="item-control config-button" data-action="modify-choices"
                       data-tooltip="N5EB.AdvancementModifyChoices"
                       aria-label="{{ localize "N5EB.AdvancementModifyChoices" }}">
                        <i class="fas fa-cog"></i>
                    </a>
                    {{else}}

                    {{#if (eq configured "full")}}
                    <a class="info-control" data-tooltip="N5EB.AdvancementConfiguredComplete"
                       aria-label="{{ localize "N5EB.AdvancementConfiguredComplete" }}">
                        <i class="fas fa-check-circle"></i>
                    </a>
                    {{else if (eq configured "partial")}}
                    <a class="info-control" data-tooltip="N5EB.AdvancementConfiguredIncomplete"
                       aria-label="{{ localize "N5EB.AdvancementConfiguredIncomplete" }}">
                        <i class="fas fa-exclamation-triangle"></i>
                    </a>
                    {{/if}}

                    {{/if}}
                </div>

            </div>

            <ol class="item-list unlist">
                {{#each items}}

                <li class="advancement-item item {{ classes }}" data-id="{{ id }}">
                    <div class="item-row">

                        <div class="item-name">
                            <img class="item-image gold-icon" src="{{ icon }}">
                            <div class="name">
                                <div class="title">{{{ title }}}</div>
                                {{#if summary}}
                                <div class="summary">{{{ summary }}}</div>
                                {{/if}}
                            </div>
                            <div class="tags">
                                {{#each tags}}
                                <span data-tooltip="{{ label }}" aria-label="{{ localize label }}">
                                    <n5eb-icon src="{{ icon }}"></n5eb-icon>
                                </span>
                                {{/each}}
                            </div>
                        </div>

                        <div class="item-detail advancement-value {{#unless value}}empty{{/unless}}">
                            {{#if value includeZero=true}}
                            {{ value }}
                            {{/if}}
                        </div>

                        <div class="item-detail item-controls">

                            {{#if @root.editable}}
                            {{!-- Editing --}}
                            <a class="item-control item-action" data-action="edit"
                               data-tooltip="N5EB.AdvancementControlEdit"
                               aria-label="{{ localize "N5EB.AdvancementControlEdit" }}">
                                <i class="fas fa-pen-to-square"></i>
                            </a>

                            {{!-- Deleting --}}
                            <a class="item-control item-action" data-action="delete"
                               data-tooltip="N5EB.AdvancementControlDelete"
                               aria-label="{{ localize "N5EB.AdvancementControlDelete" }}">
                                <i class="fas fa-trash"></i>
                            </a>
                            {{/if}}

                            {{!-- Context Menu --}}
                            <a class="item-control interface-only" data-context-menu
                               aria-label="{{ localize "N5EB.AdditionalControls" }}">
                                <i class="fas fa-ellipsis-vertical"></i>
                            </a>

                        </div>

                    </div>
                </li>

                {{/each}}
            </ol>

        </div>

        {{/each}}
    </section>
</div>
